# Chatgame Design System

## 方向

当前界面是 Truth Engine 工作台：让玩家看见自由目标、世界步骤、公开检定与可知观察之间的关系。它不是动作菜单、开发控制台或伪装成地图的卡片墙。

## 视觉语义

所有组件只消费 `--cg-*` 变量。根主题提供背景、表面、文字、弱文字、边界、主操作、强调、焦点与危险角色；组件中不得写颜色值。界面使用低彩度深色表面、清晰分隔和高可读中文字体，避免渐变、玻璃、泛光和装饰动画。

## 信息层级

- 页首说明引擎身份，并在存在会话时显示 revision、step 与世界时间。
- 没有世界时只显示事实性的空态和 ZIP 导入入口。
- 有世界但没有会话时展示可启动世界。
- 会话以事件时间线为主，输入区接受唯一的自由文本提交。
- 运行中允许“安全中断”；失败或步骤上限允许继续，不提供固定动作建议菜单。

## 交互规则

- 每个输入有可见 label，placeholder 只举例。
- 所有主操作目标至少 44px，并有明确 `focus-visible` 轮廓。
- 公开检定显示值取决于世界 disclosure：full、result_only 或 hidden。
- 错误紧邻当前工作区并使用 `role=alert`；进行状态使用 live region。
- 减少动效时取消空间位移与过渡，高对比模式保留边界。

当前界面契约的权威规格见[表现层](docs/game-design/presentation.md)。
