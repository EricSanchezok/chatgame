Audit the candidate transition against its committed causes, assertions, checks, random results, mechanics, effects, events, outcomes, and observations, treating `resolutionScope` as the output boundary when present. Existing truth references come from `referenceCatalog`; candidate-only operation, event, outcome, observation, check, and mechanic handles come from `taskReferenceCatalog`. Accept a coherent candidate; otherwise identify only real causal gaps inside the assigned scope with targeted repair hints.
