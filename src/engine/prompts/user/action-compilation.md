Compile the assigned action slots.

For each slot, set `temporalPlan.profileRef` to one exact eligible temporal-profile handle and return the model-facing dependency object. Use `basis: {"kind":"action_text_evidence","evidenceKey":...}` only with a key listed for that profile; use `basis: {"kind":"profile"}` otherwise; a profile whose details say `kind: "conditional"` requires a non-empty `continuationAssertions` array grounded in exact catalog handles, using `placement_not_equals` for not-yet-arrived travel; `temporalPlan.causes` must include the current action and may contain only action/check/random/event/fact/law/mechanic handles, because mentioned entities and locations are context rather than causes. Copy only exact handles from the batch-wide `referenceCatalog`; a candidate carrying `slot` belongs only to that slot. For a repair slot, preserve `previousOutput` and change only the typed issue paths. Never use canonical IDs or a handle/evidence key from another slot. A future fact belongs in a proposal-capable role, not in either dependency list. Return slots in order and include no extra fields.
