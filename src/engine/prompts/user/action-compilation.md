Compile the assigned action slots.

For each slot, set `temporalPlan.profileRef` to one exact eligible temporal-profile handle and return the model-facing dependency object; use `basis: {"kind":"action_text_evidence","evidenceKey":...}` only with a listed key and use `basis: {"kind":"profile"}` otherwise, while a `kind: "conditional"` profile always needs non-empty, catalog-grounded `continuationAssertions`. A continuation assertion is an invariant already true when the activity starts and true until its boundary: use `elapsed_seconds_compare` for a future time boundary, use `placement_not_equals` for travel only when the action names an exact destination placement and the actor is not already there, use an exact catalog handle or numeric comparison for `wait-until`, and never select travel-until-arrival for generic movement without a destination or submit an assertion false in the current state. `temporalPlan.causes` must include the current action and may contain only action/check/random/event/fact/law/mechanic handles because mentioned entities and locations are context rather than causes; copy only exact handles from the batch-wide `referenceCatalog`, and a candidate carrying `slot` belongs only to that slot. For a repair slot, preserve `previousOutput` and change only typed issue paths; never use canonical IDs or a handle/evidence key from another slot, and keep future facts in a proposal-capable role rather than either dependency list. Return slots in order and include no extra fields.
