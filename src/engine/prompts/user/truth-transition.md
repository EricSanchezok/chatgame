Produce one candidate transition covering every final action, using only committed plans, checks, random results, events, and the supplied temporal boundary. Describe semantic effects with relevant causes and preconditions, leaving deterministic writes and time advancement to the engine. When emitting a `mechanicInvocation`, select an active `mechanicContracts` entry and copy its exact input shape; do not invent legacy fields, raw numeric deltas, or alternate names. If a mechanic invocation is being repaired, change only the invocation identified by `resolutionScope` and preserve all other valid proposal fields.
