Produce one candidate transition covering every action in `state.actionSet.assigned`, using the committed plans and receipts in `state.committedResolutionPlans` and `state.resolutionReceipts`, plus committed checks, random results, events, and the supplied temporal boundary. Use `*Ref` fields with exact existing handles and proposal keys for new entities, facts, events, outcomes, and mechanics. Describe semantic effects with relevant causes and preconditions while leaving deterministic writes and time advancement to the engine, select an active `mechanicContracts` entry with its exact input shape when emitting a `mechanicInvocation`, never invent legacy fields or raw numeric deltas, and change only the invocation identified by `task.resolutionScope` during repair while preserving all other valid proposal fields.
