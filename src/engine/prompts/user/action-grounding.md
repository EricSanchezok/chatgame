Ground this action against the supplied canonical catalog. Return only exact canonical resources, valid Agent audiences, and listed shared pools that the action may affect; treat private evidence IDs, unknown names, and fuzzy aliases as repairable reference errors rather than a global footprint, and use global fallback only when the action truly requires world-wide arbitration with an exact world reference in reads or writes.
