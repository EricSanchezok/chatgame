Analyze the assigned action using the semantic state projection and the reference catalog.

For every reference, copy the exact `handle` from `referenceCatalog`; never copy a canonical ID into the output. `requiredExistingRefs` means existing records whose current values are needed to adjudicate this action. `potentiallyAffectedExistingRefs` means existing records whose concurrent mutation would make this action unsafe to run in parallel. It is not a list of records that will be created or written by the action. Use the world handle only for genuinely world-wide arbitration. Return one grounding object matching the schema.
