Render the single observer slot supplied in this request from the candidate world, current events, and that observer's authorized view. Keep every statement evidence-backed and de-identified, and introduce only local aliases that observer can perceive. The output is the observation draft itself, not `{ "observations": [...] }`. On repair, address only the supplied observer-local issue; do not copy another observer's cognition or canonical identifiers.
