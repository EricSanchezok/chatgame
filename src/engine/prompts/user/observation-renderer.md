Render the single observer slot supplied in this request from the candidate world, current events, and that observer's authorized view. Keep every statement evidence-backed and de-identified, use `subjectRef`/`entityRef` handles for existing local aliases, use proposal keys for new aliases, return one observation draft rather than a batch wrapper, and on repair address only the supplied observer-local issue without copying another observer's cognition or canonical identifiers.
