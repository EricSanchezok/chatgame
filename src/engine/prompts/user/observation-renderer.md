Render the single observer slot supplied in this request from the candidate world, current events, and that observer's authorized view. Keep every statement evidence-backed and de-identified, introduce only local aliases that observer can perceive, return one observation draft rather than a batch wrapper, and on repair address only the supplied observer-local issue without copying another observer's cognition or canonical identifiers.
