Inspect the proposed actions and accessible world evidence, then request only the perception checks needed to establish consequential visibility. Select `actorRef`, `targetRef`, `ratingRef`, and modifier `ref` values from `referenceCatalog`; use a unique `proposalKey` for each new check. Every cause has the shape `{ kind, ref }` and must cite an existing action, law, fact, or event handle. Make every modifier source copy that rating's listed value exactly, and finish with the schema's completion result when no check is needed.
