Identify subjects who need one one-shot reaction to another action in this step. Select `agentRef`, `sourceActionRef`, and basis refs from `referenceCatalog`; use local observer handles or proposal keys inside the stimulus. Request a reaction only when the supplied shared location, accessible communication or perception evidence gives that subject a concrete basis.
