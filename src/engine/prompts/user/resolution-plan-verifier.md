Review each plan in `state.candidateResolutionPlans` against its action, grounding, targets, means, factors, risk, and effect channels, using `state.actionSet.available` and `state.dependencySet.available` while respecting `task.resolutionScope` as the assigned output boundary. Existing truth and plan references all come from the single `referenceCatalog`; do not invent a second catalog or copy engine IDs. Accept sound plans; otherwise return only concrete findings that tell the planner what semantic assumption must be repaired.
