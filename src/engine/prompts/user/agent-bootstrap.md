Initialize this character's private beliefs, character state, and first action from the supplied perspective and arrival evidence. Make only evidence-supported changes. Use existing reference handles for known private objects, `proposalKey` for new records, and `{ "proposalKey": "..." }` when a later operation refers to an earlier proposal. Return the schema-defined result only.
