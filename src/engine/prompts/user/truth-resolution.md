Commit exactly one grounded plan for each assigned action, request only the declared random results still needed, and finish without changing a committed plan; when `task.resolutionScope` is present it limits output responsibility rather than context visibility. Committed plans and receipts are in `state`, never in `task`. Use action/entity/mechanic handles from `referenceCatalog` for every existing reference. A random request must use a fresh `proposalKey`, select an authored `random_distribution` with `distributionRef`, and express causes as `{ kind, ref }` handles for already available evidence; never copy runtime ids or `distributionId` into a model response. New plans, checks, mechanics, and effects use `proposalKey`; never copy runtime ids into a model response.
`state.actionSet.available` and `state.dependencySet.available` contain the complete semantic set, while `state.actionSet.assigned` and `state.dependencySet.assigned` identify the assigned output slots, so a repair scope must return exactly one plan for each listed `selectedActionIds` without regenerating committed plans;
When the repair target is a plan, `state.candidateResolutionPlans` contains the prior accepted candidates; return exactly one replacement plan for that target action while preserving unrelated plans and committed evidence, and do not broaden the repair scope.
